spring.application.name=proto-space

# DB接続（PostgreSQL・テスト専用DB）
spring.datasource.url=jdbc:postgresql://localhost:5433/protospace_test
spring.datasource.username=protospace_test
spring.datasource.password=changeme_test
spring.datasource.driver-class-name=org.postgresql.Driver

# Flyway（テストDBにも本番と同じマイグレーションを適用）
spring.flyway.enabled=true

# MyBatis
mybatis.configuration.map-underscore-to-camel-case=true